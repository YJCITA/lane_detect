AUX_SOURCE_DIRECTORY(. SRC_LIST)

ADD_LIBRARY(ipm STATIC ${SRC_LIST})

IF (ANDROID_ABI)
ELSEIF(WIN32)

ELSE()
#    TARGET_LINK_LIBRARIES(coordinate_trans concurrency)
ENDIF ()

INCLUDE_DIRECTORIES(${PROJECT_ROOT_PATH})
